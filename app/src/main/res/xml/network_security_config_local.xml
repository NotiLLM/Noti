<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Selected only when noti.useLocalN8n=true. The test server is intentionally plain HTTP. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
